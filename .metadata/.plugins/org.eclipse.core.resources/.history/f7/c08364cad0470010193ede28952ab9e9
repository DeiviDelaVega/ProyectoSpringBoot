<!-- archivo: src/main/resources/templates/reserva/lista.html -->
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Lista de Reservas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-4">
    <h2>Listado de Reservas</h2>

    <!-- Filtro de búsqueda por cliente -->
    <form method="get" th:action="@{/reservas/buscar}" class="mb-3">
        <div class="input-group">
            <input type="text" name="cliente" class="form-control" placeholder="Buscar por nombre de cliente">
            <button type="submit" class="btn btn-primary">Buscar</button>
        </div>
    </form>

    <a class="btn btn-success mb-3" th:href="@{/reservas/nueva}">Nueva Reserva</a>

    <table class="table table-striped">
        <thead>
        <tr>
            <th>ID</th>
            <th>Cliente</th>
            <th>Inmueble</th>
            <th>Fecha Inicio</th>
            <th>Fecha Fin</th>
            <th>Estado</th>
            <th>Acciones</th>
        </tr>
        </thead>
        <tbody>
        <tr th:each="reserva : ${reservas}">
            <td th:text="${reserva.id}"></td>
            <td th:text="${reserva.cliente.nombre} + ' ' + ${reserva.cliente.apellido}"></td>
            <td th:text="${reserva.inmueble.nombre}"></td>
            <td th:text="${reserva.fechaInicio}"></td>
            <td th:text="${reserva.fechaFin}"></td>
            <td th:text="${reserva.estadoReserva}"></td>
            <td>
                <a th:href="@{'/reservas/editar/' + ${reserva.id}}" class="btn btn-sm btn-primary">Editar</a>
                <a th:href="@{'/reservas/eliminar/' + ${reserva.id}}" class="btn btn-sm btn-danger">Eliminar</a>
            </td>
        </tr>
        </tbody>
    </table>
</div>
</body>
</html>